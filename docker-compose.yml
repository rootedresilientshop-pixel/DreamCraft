services:
  mongodb:
    image: mongo:latest
    container_name: venturelab-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: dreamcraft
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
